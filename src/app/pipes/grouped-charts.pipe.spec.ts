import { GroupedChartsPipe } from './grouped-charts.pipe';

describe('GroupedChartsPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupedChartsPipe();
    expect(pipe).toBeTruthy();
  });
});
