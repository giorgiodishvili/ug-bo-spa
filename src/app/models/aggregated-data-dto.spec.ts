import { AggregatedDataDto } from './aggregated-data-dto';

describe('AggregatedDataDto', () => {
  it('should create an instance', () => {
    expect(new AggregatedDataDto()).toBeTruthy();
  });
});
